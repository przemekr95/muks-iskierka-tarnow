import React from 'react';
import Navbar from '../navbar/navbar';

import './header.scss';

const Header = () => {
  console.log('rser');
  return (
    <header>
      <Navbar />
    </header>
  );

};

export default Header;
