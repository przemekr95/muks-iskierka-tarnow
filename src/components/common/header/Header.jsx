import React from 'react';
import Navbar from '../navbar';

function Header() {
  return (
    <section>
      <section>
        <a href="/">logo</a>
      </section>
      <section>
        <Navbar />
      </section>
    </section>
  );
}

export default Header;
